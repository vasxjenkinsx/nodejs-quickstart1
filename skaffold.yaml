apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/vasxjenkinsx/nodejs-quickstart1:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/vasxjenkinsx/nodejs-quickstart1:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: nodejs-quickstart1
        chartPath: charts/nodejs-quickstart1
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/vasxjenkinsx/nodejs-quickstart1'
          image.tag: '{{.DIGEST_HEX}}'
